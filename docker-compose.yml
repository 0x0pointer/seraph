version: "3.9"

services:
  seraph:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - SERAPH_CONFIG=/app/config.yaml
    restart: unless-stopped
