# Test configuration — no API keys (open mode), tiers disabled, no scanners
listen: "0.0.0.0:8000"
upstream: "https://api.openai.com"
api_keys: []

logging:
  level: warning
  audit: false

nemo_tier:
  enabled: false

judge:
  enabled: false

scanners:
  input: []
  output: []
