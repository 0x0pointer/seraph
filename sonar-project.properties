sonar.projectKey=0x0pointer_seraph
sonar.organization=0x0pointer

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=seraph
#sonar.projectVersion=1.0

# Source paths
sonar.sources=frontend/src,backend/app,chatbot

# Exclusions
sonar.exclusions=**/node_modules/**,**/.next/**,**/dist/**,**/__pycache__/**,**/migrations/**,**/seed*.py

# Python version
sonar.python.version=3.11

# Coverage reports
sonar.python.coverage.reportPaths=backend/coverage.xml,chatbot/coverage.xml
sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
